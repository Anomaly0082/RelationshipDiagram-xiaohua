在修改图片对应的json文件时，需要注意的是，图片的大小会影响到节点的大小，因此需要根据图片的大小来修改json文件中的size属性。
G6中节点的size属性是一个数组，数组的第一个元素是节点的宽度，第二个元素是节点的高度。
另外,G6要设置不用类型的结点比较困难,需要继承一个类,然后在类中重写方法,根据不同的类型来绘制不同的节点,所以最简单的想要修改结点样子的方法就是直接修改图片(当然其实没必要修改了,难看点也无所谓)
另外人物的信息还要补充一下,现有的信息比较少,比如人物的生卒年,人物的职务,人物的重要性等,这些信息都可以补充一下,这个可以自由发挥,无所谓